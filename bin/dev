#!/usr/bin/env bash
set -e

# No separate asset-watch process is needed (Sprockets compiles JS/CSS
# on-the-fly in development) so this is a plain server boot, not a
# foreman/Procfile.dev multi-process runner.
cd "$(dirname "$0")/.."
exec bin/rails server "$@"
